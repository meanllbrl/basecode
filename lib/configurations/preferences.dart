import 'package:flutter/material.dart';

class Preferences {
  //local language codes that are exist in lang folder
  static List<Locale> ADDED_LOCALES = [Locale("en"), Locale("tr")];
  //supported local coddes
  static List<String> SUPPORTED_LOCALES = ["tr", "en"];
}
